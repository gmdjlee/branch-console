package com.branchconsole.engine

/**
 * MT1-01a 스캐폴드 placeholder. 실제 엔진 로직(transforms·scoring·statemachine)은
 * 후속 서브태스크(MT1-05)에서 채워진다. `:engine`이 Android API에 의존하지 않는다는
 * 아키텍처 제약(docs/plans/M1_PLAN_A.md AD-A1)을 검증하는 최소 컴파일 단위로 둔다.
 */
object EngineInfo {
    const val MODULE_NAME: String = "engine"
}
