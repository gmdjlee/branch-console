# REVIEW_M1 — M1 phase 판정 로그

- 시작: 2026-08-06 · 기록: Advisor(aaa-critic 판정 원문 전재·요약) · 규율: AAA_QUALITY_STANDARD §1·§5
- 대상 phase: Track M / M1 모바일 코어 (TASK_mobile_m1.md)

---

## Plan Council (AAA §3)

- 공통 브리프: `docs/plans/M1_COUNCIL_BRIEF.md` (2026-08-06)
- 계획 3안: A 아키텍처(`docs/plans/M1_PLAN_A.md`) / B 데이터·정합성(`M1_PLAN_B.md`) / C UX·운영(`M1_PLAN_C.md`)
- 작성: plan-architect ×3 병렬(claude-opus-5, xhigh) · 판정: aaa-critic(claude-opus-5, xhigh)

### 라운드 1 (2026-08-06) — A: FAIL / B: FAIL / C: FAIL

비평가 검증 방식: 3안 인용 사실을 저장소 직접 대조(configs·engine_ref·backtest·contracts·
`D:\android_2025\kotlin_krx`), 회귀 게이트 직접 재실행(`ruff` All checks passed / `pytest` 177 passed).

#### PLAN A — FAIL (중대 3 · 중 3 · 경미 2)

| # | 등급 | 결함 | 근거 |
|---|---|---|---|
| A-1 | 중대 | **프로덕션 스테일 기준 시각(visible_at) 산출 규정 부재** — engine_ref `is_stale`은 as_of 기준이나 run_replay `is_stale_check`(L352-367)는 visible_at 기준(실측 재현 중 발견 명시). A는 패리티 입력으로 visible_at을 Python에서 주입받을 뿐, 앱 실운영의 visible_at 산출 규칙이 계획 전체에 없음 | §2.1 PIT·패리티, 브리프 §5-8 |
| A-2 | 중대 | **캐치업 as-of 쿼리의 `observed_at <= evaluatedAt` 절이 캐치업을 무효화(내부 모순)** — 놓친 날 D의 데이터는 오늘 수집돼 observed_at=오늘. evaluatedAt=과거면 전 행 배제→전 지표 결측→D-25 §3 동결. evaluatedAt=now면 증인 테스트·R-06 공허. 캐치업 틱의 evaluatedAt 미정의 — MT1-03a와 MT1-06b 완료 기준 양립 불가 | §2.2 멱등, §2.1 look-ahead |
| A-3 | 중대 | **억제 임계 0.80의 SSOT 위치 없음 + §11 신설 제안도 없음** → 착수 즉시 CLAUDE.md §1/AAA §2.3 위반 확정 | §2.3 SSOT |
| A-4 | 중 | K-04 ECOS item_code 위치 오기 — `VERIFY`는 sources.yaml이 아니라 `indicators.yaml` `krx_credit_spread_delta.source.item_codes`. §11 변경 제안에 indicators.yaml 대상 0건 | 실측 grep |
| A-5 | 중 | D-23 §23.2의 45.2를 "이월 후"로 오기재 — 45.2는 전체 분모(서버 동시각) 값. 이대로면 완료 기준 ④가 D-23 명제를 증명 못 함 | ARCHITECTURE_SPLIT 원문 |
| A-6 | 중 | AD-A7(17:00)이 의존하는 물리 사실("17:00에 지수 종가·순매수·VKOSPI 확정")의 실측 과업이 MT1-00에 없음 — AD-3b "동시 재확인" 조건 미이행 | 브리프 §2-3 |
| A-7 | 경미 | `catchup_max_trading_days` 30 산출 근거 없음 | — |
| A-8 | 경미 | kotlin_krx 오류 정책(파싱 실패·휴장 모두 빈 리스트) 미흡수 — 04c·06b 구분 장치 없음 | kotlin_krx CLAUDE.md L214·216 |

#### PLAN B — FAIL (중대 3 · 중 2 · 경미 2)

| # | 등급 | 결함 | 근거 |
|---|---|---|---|
| B-1 | 중대 | **AAA §2.3 라인 커버리지 기준(코어 ≥90%/기타 ≥70%)의 측정·강제 태스크가 계획 전체에 부재** (kover/jacoco 0건) | §2.3 |
| B-2 | 중대 | 억제 임계 0.80의 SSOT 위치 미정의(§15 제안 5건에 키 신설 없음) | §2.3 SSOT |
| B-3 | 중대 | **프리뷰 coverage의 이월 계상 여부 미정의** → 이월값이 유효가중에 계상되면 <80% 억제 미발동, TASK MT1-07 완료 기준 ③(67.7% 억제) 재현 불가. §14 결정 목록에도 없음 | D-23 §23.3 |
| B-4 | 중 | 부정 케이스 `invalid/naive_datetime.json`("양측 거부")이 현행 contracts에서 통과 불가 — pydantic datetime은 naive 수용, AwareDatetime/validator 0건, §15 제안에도 없음 | 실측 grep |
| B-5 | 중 | kotlin_krx 컴포지트 빌드 권고에 재현성 해법 부재(외부 절대경로·커밋 핀·FAIL_ON_PROJECT_REPOS 상호작용·플러그인 버전 정렬 미규정) | §2.3 재현성 |
| B-6 | 경미 | 캐치업 상한 없음 — 자체 R-B9(K-03 과호출 차단)와 상충 | — |
| B-7 | 경미 | MT1-06a 위임이 Advisor — §16.2 "전건 2단 판정" 선언과 모순 | CLAUDE.md §0 |

#### PLAN C — FAIL (중대 3 · 중 2 · 경미 2)

| # | 등급 | 결함 | 근거 |
|---|---|---|---|
| C-1 | 중대 | **BT-05·PIT 설계가 실행 가능 수준으로 부재** — visible_at 언급 0건(스테일 기준 시각 미반영 → 결측 낀 전 틱 패리티 파손), 픽스처 주입 규격·계층 판정 기준·창 범위 없음 | §2.1, 브리프 §4 |
| C-2 | 중대 | **해시 검증에서 TASK 명시 요구(계측 테스트 SHA-256) 축소** — U-7(b)가 Robolectric 대체·계측 격하. "MT1-01~08 축소 불가" 위반 | 브리프 §1·§4 |
| C-3 | 중대 | SSOT 변경 제안 P-2(우선순위 최상, MT1-04d 블로커)가 **존재하지 않는 키**(sources.yaml item_codes)를 대상 — 정답은 indicators.yaml | 실측 |
| C-4 | 중 | 캐치업 상한을 "판정 무관"으로 분류 — 상한 밖 거래일 영구 누락은 히스테리시스 틱 카운트를 바꾸는 판정 영향값. 자체 원칙("판정 영향값만 SSOT")과 상충 | statemachine.yaml |
| C-5 | 중 | 모듈 경로 내부 불일치 — 모듈 목록에 없는 `:core:krx`가 완료 명령에 등장(실행 불가) | 브리프 §4-② |
| C-6 | 경미 | coverage 상한 표기 불일치(95.16% vs 95.2%) — 표시 반올림 규칙(K-07) 부재 | — |
| C-7 | 경미 | 스모크가 게이트 조건(확정 1회+프리뷰 3회)을 크게 초과(사용자 4시간 분산)하면서 사용자 승인 항목으로 미상신 | — |

#### 상호 모순 (병합 시 해소 대상)

- **구조적**: X-1 모듈 레이아웃 3원화(전 완료 명령이 모듈 경로 하드코딩 — 병합 시 일괄 재작성 필수) /
  X-2 assets 물리 위치(A의 `verifyNoCheckedInAssets` ⊕ B·C의 assets 직접 복사 — 직접 배타) /
  X-3 캐치업 observed_at 필터(A 필수 증인 ⊕ B 미사용 — 동시 통과 불가, A-2와 동근) /
  X-4 kotlin_krx 통합(A 벤더링+적응수정 ⊕ B 컴포지트 ⊕ C 벤더링+수정금지) /
  X-5 프리뷰 coverage 이월 계상(A raw ⊕ C carry-forward 반영 — C는 내부 충돌도 있음) /
  X-6 해시 검증 소스셋(C안은 TASK 축소라 채택 불가)
- **권고 차이**(Y-1~Y-10): 확정 틱 17:00(3안 일치, 실측 게이트 유무만 상이) / confirm_time SSOT 위치 /
  BT-05 범위(B의 9창+합성 증인 근거 실측 확인됨: 골든 최대 낙폭 15.557 < extreme 20.0 → 골든만으로는
  or_any_extreme·D-26 0회 발화) / VKOSPI(실질 수렴: 수집·기록하되 v1 판정은 폴백) / G-4 (b) 일치 /
  캐치업 상한(30 vs 무제한 vs 10) / Drive 백업 / M1 UI 위임(ui-craftsman의 과업 정의가 §2.4 "M2~"와
  불일치) / run_log purge / 아키텍처 테스트 도구
- **Advisor 필수 결정 30건(M-01~M-30)** 목록은 비평 원문(라운드 1) 보존 — 병합 시 전건 처리.
  특히 M-09(0.80 SSOT)·M-12(visible_at)·M-13(커버리지 게이트)은 2안 이상 동시 누락 — 재위임 브리프에
  명시 요구로 반영(아래).

#### 처리

3안 전원 반려·재작성 위임(라운드 2). 반려 브리프에 각 안의 결함 전건 + 공통 명시 요구 3건
(①억제 임계 0.80의 SSOT 신설 제안 ②visible_at 프로덕션 산출 규칙 ③커버리지 게이트 도구·강제)을 포함.
상호 모순 X·Y는 병합 단계 해소 예정이나, 각 안의 **내부** 모순(A-2, C-5, C의 X-5 내부 충돌)은 반려 사유.

### 라운드 2 (2026-08-06) — A: FAIL / B: FAIL / C: FAIL

- **라운드 1 결함 22건 전건 실질 해소 확인**(비평가 파일 직접 재검증, 회피성 서술 0 — 3안 모두
  오류를 인용부호로 명시 철회). 재작성 중 유입된 신규 결함으로 전원 재FAIL.
- 신규 결함의 공통 뿌리: `engine_ref/statemachine.py` `run(ticks, profile, config)`은
  `phase = config.initial_phase`(L114)로 시작하며 시작 국면·`promote_streaks`·`demote_streak`·
  `cooldown`·`ticks_in_phase`를 주입받는 파라미터가 없다 — 이 API 사실이 3안 어디에도 반영되지 않았다.

| 안 | 신규 결함 |
|---|---|
| A | **A-9(중대)** 상태기계 프로덕션 호출·상태 지속 모델 부재(tick_run 스키마에 distinct_axes·any_crit·any_extreme 없음 → fold 재구성 불가, "직전 국면+오늘 1틱"은 현행 API로 표현 불가. A-1과 동일 유형 2회 연속 — 3회 시 구조 문제 재분류 예고) / **A-10(중)** stale 등호 규약 미기재(정본: 초과만 stale, 등호 fresh — registry.py:323) / **A-11(경미)** 캐치업 상한 SSOT 대상 파일 부적합(sources.yaml → statemachine.yaml profiles가 동종 파라미터 위치) |
| B | **B-8(중대)** 캐치업 절단 "스트릭·cooldown 리셋"이 자신의 전량 fold 설계·엔진 API 양쪽에서 성립 불가("tick_input 구성 규칙" 주장 반증 — 리셋은 run() 변경 없이 구현 불가, MT1-06e 완료 기준 실행 불가) / **B-9(중)** `:app` Compose 커버리지 M1 한정 제외 = AAA §2.3에 없는 자기 유예(결정 상신도 없음. A는 정반대로 "UI 제외 금지" 명시) / **B-10(중)** 2계열 지표 결합 가시 시각(worst-of-inputs) 누락 — global_corr_break에서 패리티 확정 실패 |
| C | **C-8(중대)** visible_at 컬럼 저장 — 자기 §9-B-1(순수 함수)과 모순, revision 의미론 파괴, 과거 틱 결정론 파손(A·B는 "저장 시 grid 정정 드리프트"를 정확히 예측) / **C-9(중대)** 상태기계 지속 모델 부재(A-9 동종. 단 리셋 의미론은 U-6·P-12로 상신해 결정 사항임을 인정 — B보다 정직) / **C-10(경미)** 캐치업 상한 10 = 히스테리시스 상수 합(3+5+2)과 동일, 여유 0 |

- 상호 모순 갱신: **해소·수렴** X-3(observed_at 필터→visible_at), X-4(통합=벤더링), X-5(coverage
  raw 억제), X-6(계측 해시 정본) / **잔존** X-1(모듈 레이아웃 3·5·7 — 병합 결정), X-2(assets 물리
  위치 직접 배타), X-4b(벤더 수정 허용 여부 — C 래퍼 흡수로 봉합 가능) / **신규 구조** X-7(visible_at
  저장 vs 파생 — C 이탈), X-8(상태기계 지속 모델 — B만 전량 fold 정의)
- visible_at 의미론 3안 대조(패리티 직결): 계열별 3규칙(us_market after / fred +lag on_or_after /
  krx·fx on_or_after)은 3안 정확 일치. 이탈 4축 — C 저장(X-7), B worst-of 누락(B-10), A 등호 침묵
  (A-10), C cadence 폴백 누락(경미). 캐치업 evaluatedAt=D 17:00은 A만 명문화.
- M-xx 갱신: 수렴 해소 M-02·04·07·08·10·12·13·15(+M-11 부분) / 신설 M-32(저장 vs 파생),
  M-33(지속 모델), M-34(절단 리셋의 지위 — 엔진 변경 승인 여부), M-35(UI 커버리지 제외),
  M-36(stale 등호 명문화), M-37(worst-of-inputs), M-02b(벤더 수정·경로) / 잔존 M-01·03·05·06·09b·
  11b·14·16·17·19~31.

#### 처리 (라운드 3 위임)

3안 반려·재작성 위임. 비평가 지정 공통 명시 요구(재발 방지): `run()` API 사실을 전제로
(a) 프로덕션이 국면을 날마다 어떻게 이어받는지 (b) 근거 데이터를 어느 테이블 어느 컬럼으로
보존하는지 (c) 캐치업 절단 카운터 처리를 엔진 변경 없이 표현 가능한지 — 불가하면 엔진 변경
제안으로 정식 상신하는지 — 를 각 계획이 명시할 것.

### 라운드 3 (2026-08-06) — A: FAIL / B: FAIL / C: FAIL + **구조 문제 재분류 발동(AAA §1)**

- **라운드 2 결함 9건 전건 해소 확인**(비평가 엔진 코드 직접 추적 포함):
  - A-9·C-9·B-8: 3안 모두 "동결 `tick_input` 전량 fold + Tick 4필드 1:1 스키마"로 수렴 — 엔진
    API 사실과 정합 확인. B의 "리셋 불필요" 정량 논증(promote_sustain=1 → 이월 영향 0틱,
    demote 가속 ≤2틱, cooldown 지연 ≤2틱, 공백=휴장 동형) **코드 추적 전건 참**.
  - C-8: cutoff 역산(krx·fx `as_of≤D` / us `as_of<D` / fred `≤D−lag` / 2계열 max→min 반전)
    **수학적으로 정확** 판정. A-10 stale 등호(초과만 stale)·A-11 상한 위치·B-9 UI 제외 철회·
    B-10 worst-of-inputs(15종 매핑)·C-10 상한 20 — 전건 해소.
- **신규 결함**:
  - **N-1(3안 공통, 중대)**: transform 입력 시계열 조회 계약 부재 — 3안 모두 as-of "최신 1행"
    조회를 규정하나 zscore(252)·drawdown(60)·rolling_corr(120) 등은 원계열 창 전체가 필요.
    정본 순서(run_replay.py:289-317: 원계열 전체 transform → 출력에 가시성 색인 → 틱별 lookup)와
    반대이거나 공백. 필터-후-transform은 다른 값을 내므로 BT-05 L1/L2 확정 실패.
  - A-12(경미): 증인 W-S1 비공허성 미명시(promote_sustain=1이라 D6 단독으로도 AMBER 가능).
  - C-11(중대): observation 스키마 `(indicator_id, as_of)` 키 — 2계열·다필드(KRW=X high/low/
    close, trading_value+daily_return) 표현 불가, ^GSPC 공유 지표 귀속 모호. A·B의
    `(series_id, field, as_of)`가 픽스처 롱포맷과 일치하는 올바른 granularity.
  - C-12(중): "패리티가 프로덕션 경로를 그대로 덮는다"(a) ⊕ P-13 부분 fold(c) 모순 — 절단
    경로는 BT-05 미검증.
  - C-13(중대): "프리뷰는 observed_at 시계" — 인접 문단 자기 반박, 죽은 계열(^MOVE 절단)이
    영원히 fresh가 되는 실패 모드, D-23 §23.3-1 배지 요건 위배, A의 AT-7·B의 "프리뷰 무영향"
    보장과 정면 충돌.
- 캐치업 절단 3파전 판정: A(NULL 동결)≡B(틱 부재) — 관측 가능 귀결 완전 동치·엔진 무변경.
  C(P-13 리셋)만 다른 타임라인 — 편익 ≤2틱 vs 비용 = engine_ref 시그니처 변경+BT-05 미검증
  경로. 비용-편익 불성립 판정.
- 상호 모순 갱신: X-7·X-8 해소(3안 파생·전량 fold 수렴) / 신규 X-9(프리뷰 시계 — C 분기),
  X-10(원장 granularity — C 이탈) / 잔존 X-1·X-2·X-4b.
- M-xx: 해소 M-32·33·36·37, M-17 값 20·파일 수렴(키명만 상이) / 신설 M-38(조회 계약)·
  M-39(프리뷰 시계)·M-40(granularity)·M-41(P-13 시 BT-05 커버리지).

#### 구조 문제 재분류 (AAA §1 3연속 FAIL 조항)

결함 ID는 라운드마다 달랐으나 동일 항목의 3회 연속 FAIL로 판정: **"프로덕션 런타임 데이터
경로의 미정의"** — R1 visible_at 산출처(A-1·C-1) → R2 상태기계 지속 모델(A-9·B-8·C-9) →
R3 transform 입력 조회 계약(N-1, 3안 전원). 사슬(`원장 조회 → 시계열 구성 → transform →
가시성/스테일 → Tick 조립 → fold`)의 비어 있는 마디가 매 라운드 하나씩 발견되는 패턴.
진단: 브리프 §6 관점 분할(아키텍처/데이터 정합성/UX·운영)에 이 사슬을 소유하는 관점이 없음.
비평가 권고: ① 병합 전 6단계 런타임 데이터 경로 계약을 단일 문서로 확정(정본은 전부 실재 —
새 설계가 아니라 이식 대상의 명세화) 후 3안이 참조 ② 또는 4번째 관점 신설 라운드 4.
**Advisor가 사용자에게 보고·결정 상신**(MT0-03 선례: 3연속 FAIL 시 사용자 승인 표적 수정).

**사용자 결정(2026-08-06): 권고안 ② 채택 — 4번째 관점 "런타임 데이터 경로" 신설.**
브리프 §6에 관점 D를 추가하고 plan-architect 신규 인스턴스가 완전한 계획(M1_PLAN_D.md)을
작성한다. A·B·C는 라운드 4에서 자기 잔여 결함(A: N-1+A-12 / B: N-1 / C: N-1+C-11~13)을
수정하며, N-1 수정은 정본 순서(run_replay.py:289-317)를 기준으로 한다. 라운드 4 판정은
4안 전체 + 상호 모순 검사.

### 라운드 4 (2026-08-06~07) — A: FAIL / B: FAIL / C: FAIL / D: FAIL + **구조 재분류 종결**

(판정 중 세션 한도 중단 1회 — 동일 인스턴스 재개로 완료)

- **라운드 3 결함 6/6 전건 해소**: N-1의 세 해소 방식(A 3단계 계약 / B N행 파생 최대 272 /
  C loadSeries+Konsist) **모두 정본과 등가** 판정. C-11(granularity)·C-12(P-13 철회)·
  C-13(visible_at 단일 시계)·A-12 해소.
- **A의 지뢰 7 = 라운드 4 최대 발견**: `lookup_known`은 `bisect_right(...)−1`이라 등호 포함 —
  mobile_daily KRX·FX는 visible_at == evaluated_at(17:00 정확 일치)이므로 bisect_left/`<`로
  구현하면 **KRX 4지표+KRW=X가 매 틱 소실**. 증인 W-K1, 병합본 필수 편입(M-48).
- **D안 판정**: 수학 주장 3건(전치 정리·cutoff 역산 보조정리·3규칙→1규칙 환원) **전건 참**,
  인용 정본 전건 일치, D-D5(하니스 calendar_kind 폴백의 ECOS·CDS us_market 오분류 — 값은
  우연히 동일·근거 오류 잠복) **실측 재현 확인**. 결함: D-1(중 — 브리프 §5 Q12·Q13 미답,
  GM1 증빙 경로 미정의) / D-2(경미 — requiredRows 산술 1건).
- **신규 결함(공통 뿌리 = D-17 §3 "프리뷰 수집치도 lake에 append" 의 귀결 미처리)**:
  A-13(중) 프리뷰 적재 행이 확정 수집 실패일에 종가로 동결되는 오염 경로 무방비 /
  B-11(중) 프리뷰 evaluated_at 미정의 / B-12(중) ROW_NUMBER() OVER가 minSdk 29 미실측
  의존(C는 정반대 단정 C-16, D만 실측 과업 신설) / B-13(경미) 오염 경로 의도적 허용 /
  C-14(중) 웜업 550의 SSOT가 하니스 전용 windows.yaml — 앱이 읽을 수 없음, 신설 제안도
  없음(라운드 2 confirm_time과 동일 유형 재발) / C-15(중)=A-13 동일 / C-17·18(경미).
- **수렴 완료**: granularity·N-1 순서·visible_at 파생·stale 등호·worst-of·fold 모델·
  엔진 무변경(P-13 철회)·0.80 위치·캐치업 상한 20/statemachine.yaml·계측 해시. 절단 표현
  A(NULL 동결)≡B·C(틱 부재) **동치 확정**(statemachine.py:120-124 continue 추적).
- **프리뷰 시계 C≠D 판명**: 가시 판정은 동일하나 나이 산식이 다름(C=24h 배수 / D=실경과) —
  저녁 프리뷰에서 daily_us 30h 창의 억제 여부가 갈림. 실질 결정 사항(M-39).
- **구조 재분류 종결**: R1~R3 반복 항목(런타임 데이터 경로)은 4안 전부 종결 — 관점 D 신설
  처방 작동, 추가 에스컬레이션 불요. **신규 감시 패턴 등록**: 프리뷰 경로 의미론 2라운드
  연속 결함원(R3 C-13 → R4 A-13·B-11·B-13·C-15) — 라운드 5 공통 필수 요구로 차단.
- M-xx: 해소 M-04·11·32·33·34·36·37·38·40 / 잔존 M-01·03·09b(키명 3:1)·M-17b(키명 3:1)·
  M-42★(웜업 방식) / 신설 M-43★(프리뷰 행 배제 방식)·M-39★(프리뷰 나이 산식)·M-44(SQLite
  윈도 함수 실측)·M-45(부트스트랩 게이트)·M-46(WARMUP_INSUFFICIENT 구분)·M-47(L 매핑 위치)·
  M-48(bisect 증인)·M-49(tick_input 감사 컬럼 합집합).

#### 처리 (라운드 5 위임)

4안 재작성 위임: A(A-13) / B(B-11·12·13) / C(C-14~18) / D(D-1·2). 4안 공통 필수 요구:
**D-17 §3의 귀결 — 프리뷰 적재 행이 확정 틱 조회에 잡히지 않음을 SQL 수준에서 명시 +
프리뷰 evaluated_at 정의**(감시 패턴 차단).

### 라운드 5 (2026-08-07) — A: FAIL / B: FAIL / C: FAIL / **D: PASS** + 구조 재분류 2차 발동

- **라운드 4 결함 8/8 전건 해소**. 공통 요구 이행 4/4: 판별자(A·D lane 정수 / C lane 문자열 /
  B origin 3값 — 기전 동일), 프리뷰 evaluated_at=now 4안 일치. 비평가 검증: B의 "가시=날짜·
  스테일=시각 분리, 확정 틱 동치 → BT-05 재생성 불요" **참** / B의 격자 시각 기각 정량(오전
  프리뷰 US·FRED 14.5/31=46.8% 상실 → 상시 억제) **참** / D의 "서브쿼리 양쪽 필터" 논리 **참**
  (한쪽만 걸면 as_of 구멍 → 롤링 창 전체 이동).
- **M-39 판정**: C의 24h 스냅 잔류는 내부 건전(대가 7h 정직 계상) — 결함 아닌 병합 선택.
  실경과 3:1 우세, B 논거 3(경보 비대칭 비용 — 억제 쪽 오류가 낫다)이 결정적. 단 A의 논거 3은
  **사실 오귀속**(A-14): 48h·96h는 24h 배수, 스냅 손상은 30h뿐이며 30h는 BT-03 스윕 미대상.
  (부기: 본 라운드 기록 중 "A-1 재계산 0.847 검산 일치"는 → 2026-08-08 정정 참조(§W0 A-1
  사후 정정) — 검산 자체가 오류 수치 위에서 수행된 것이었음.)
- **신규 결함**: A-15(중) previewSeries tie-break `lane DESC` 역방향 — 확정 틱 후 프리뷰가
  13:00 부분봉으로 종가를 밀어냄(C·D는 확정 우선으로 정상) / B-14(중)·C-19(중) carry-forward
  원천의 판별자 필터 미지정 — 프리뷰가 자기 프리뷰 값을 이월(D-23 §23.3-1 "직전 **확정**값"
  위반. A는 lastConfirmed lane=0 + W-P2로 정상, D는 tick_input 원천이라 구조적 면역).
- **구조 재분류 2차(AAA §1)**: "프리뷰 경로의 원장 접근 규율" 3연속(R3 C-13 → R4 A-13·B-11·
  B-13·C-15 → R5 A-15·B-14·C-19). 원인: 프리뷰 경로의 원장 읽기 지점이 3개(①확정 틱 조회
  ②프리뷰 신선분 조회 ③carry-forward 원천)인데 A·B·C가 열거 없이 지점별 대응. D만 §2.8에서
  3지점을 표로 열거해 이 유형의 결함이 구조적으로 없음(D PASS의 원인).
  **비평가 처방(단일): D §2.8의 "읽기 지점 3개 × 판별자 값 × 증인" 표(M-43b)를 4안 공통 필수
  산출물로 지정** — 각 안이 자기 스키마 어휘로 채운 뒤 라운드 6 진행.
- 병합 필수 결정 12건 최종 목록(잠정 — 6.2)과 안별 고유 기여 흡수 목록(6.3: A 지뢰 7·DAO 봉인,
  B requiredRows 파생·backfill 지위, C 실패 경로 카탈로그·P-14 논증, D 수학 3정리·L 표·읽기
  3분기·진단 JSON) 비평 원문 확보 — 병합 단계 입력.

#### 처리 (라운드 6 위임 — 표적 수정)

Advisor 판단: 처방이 단일하고 잔여 결함 3건이 전부 M-43b 표의 빈칸이므로 표적 수정으로 진행
(구조 재분류는 본 로그와 세션 보고로 사용자에게 보고됨, GATE_GM1에 재기록 예정).
A(A-15 tie-break 정정 + A-14 논거 정정) / B(B-14 이월 필터) / C(C-19 lanes 고정) +
3안 공통 M-43b 표 작성(D §2.8 정합). D는 재작성 불요(PASS 유지) — 라운드 6에서 정합 확인만.

### 라운드 6 (2026-08-07) — A: FAIL / **B: PASS** / C: FAIL / **D: PASS**

- **표적 수정 전건 해소**: A-15(lane ASC + W-P5 퇴화 증인)·A-14(철회 블록 + §2.8 소급 정정,
  재검산 정확)·B-14(이월 원천을 tick_input 행으로 전환 — 구조적 면역, 호출자 실수 표면 0)·
  C-19(lanes {CONFIRM} 타입 수준 고정). B의 부수 발견 R-B24(자기 tie-break의 A-15 동형 결함
  자기 적발·봉쇄)도 확인. M-43b 표 3건 모두 D §2.8과 정합(①② 판별자·tie-break 4안 일치).
  기해소분 회귀 표본 점검: 되돌림 0건.
- **신규 결함(동형 2건)**: A-16(중)·C-20(중) — 읽기 지점 ③의 **반환 계층 단절**. A·C의 이월
  원천은 observation 재조회로 **원계열 값**을 반환하는데 소비자(compute_composite)는
  **severity**를 받는다. 도출 경로 부재 — 두 경로 모두 막힘(원값 직접 분류=오류, 원계열
  재주입=전치 등식 위반). B·D는 원천이 tick_input.severities라 구조적 면역.
- **구조 재분류 상태**: 처방(읽기 지점 전수표)이 작동 — lane 축 완전 폐쇄. A-16·C-20은 같은
  표의 미열거 축(반환 계층). 새 에스컬레이션 아님 — **표에 "반환 계층" 열 추가**가 처방.
- **병합 입력 확정본 수령**(비평 원문 §5): 수렴 완료 17항목 / 필수 결정 13건(M-01·03·09b·17b·
  34·39·42·43·43b·44·45/46·49·50 — 각 권고 포함, M-43b는 B·D의 tick_input 원천 권고·
  M-50 신설 severities_json 승격) / 안별 고유 기여 흡수 목록 14항목.

#### 처리 (라운드 7 위임 — 표적 2건)

A(A-16)·C(C-20): M-43b 표에 "반환 계층" 열 추가 + ③을 tick_input.severities 원천으로 전환
(권고) 또는 도출 경로를 자기 계약과 모순 없이 명시. B·D 재작성 불요 — 라운드 7은 정합 확인만.

### 라운드 7 (2026-08-07) — **A: PASS / B: PASS / C: PASS / D: PASS — 전원 PASS, 판정 단계 종결**

- A-16·C-20 해소 확인: 4안 모두 이월 원천 = `tick_input`(severity 계층·확정 전용 구조적 보장·
  레인 필터 불요)으로 완전 수렴. M-43b 표에 "반환 계층" 열 4안 정합. 신규 결함 0, 회귀 0.
- A의 `composite IS NOT NULL` 필터는 결함이 아니라 정제로 판정 — 병합 하위 결정 M-43b-i로 승격.
- **병합 입력 최종 확정본 수령**: 수렴 완료 20항 / 필수 결정 11건 + 이월 하위 3항(M-43b·M-50은
  수렴으로 결정 목록에서 제외) / 고유 기여 흡수 16항 / 병합 유의 4항(서브태스크 합집합·SSOT
  제안 통합·착수 선행 2건·메타데이터 정리).
- 누계: 라운드 7회, 판정 결함 총 40건(R1 22 → R2 9 → R3 6+ → R4 8 → R5 4 → R6 2 → R7 0),
  구조 재분류 2회(관점 D 신설 / M-43b 전수표) — 두 처방 모두 작동 확인.

## 병합 (AAA §3-4)

Advisor 병합 결정·정규 참조 매핑·서브태스크 합집합: `docs/plans/M1_PLAN_FINAL.md` (2026-08-07).
사용자 승인(AAA §3-5) 후 착수. 승인 시 확정: KIS 미보유→04e M2 이연 / 스모크 필수만+S9.

---

## 구현 단계 판정 로그

### W0 실측 선행 묶음 (MT1-00a~d·g + SSOT 선행 2키) — 커밋 ea14a87·63d4cf0·87e059c·30195ef·3012100·a7b0322·523065d

**라운드 1 (2026-08-07)**: qa-verifier **PASS**(발견 0, 경미 관찰 2 — probe 저장 KST-aware·기존
ruff format 드리프트) → aaa-critic **FAIL** (치명 1·중 1·증인 1·SSOT 1·경미 1):

| # | 결함 | 요지 |
|---|---|---|
| D-1 | 치명 — 00c 투자자 순매수 필드 오정렬 | MDCSTAT02203 TRDVAL8~11이 한 칸씩 밀림(TRDVAL8=기타법인). 저널이 "외국인"으로 인증한 값 = 실제 기타외국인(부호 반대·~203배). 외국인합계=TRDVAL10+11. kotlin_krx 자신의 fromTickerJson KDoc이 정답 명기. pykrx 독립 대조로 반증. weight 2.0 foreign_net_sell_kospi 직결. 시장 스코프도 spec=KOSPI vs 실측=ALL |
| D-2 | 중 — total=0 오단정 | 순매수 11분류 합=0이 참값(pykrx 대조 확인). "필드 매핑 결측" 단정·재계산 처방은 검산 시 +3,938억(≠0)으로 자기모순 |
| D-3 | 증인 — 00g 뮤턴트 생존 | df.index[-1]→[0] 변이가 6/6 통과(픽스처 전부 1행). 파손 시 영구 not_yet_today → confirm_time SSOT 오염 경로 |
| D-4 | SSOT — 00g 레이트리밋 하드코딩 | PYKRX_MIN_INTERVAL_S=1.0 리터럴. 정본은 sources.yaml(선례 build_fixtures.py:193) |
| D-5 | 경미 — 00g 저널 표본 계획 오기 | 6시점 사전등록을 7/8시점으로 3회 상이 기재 — 게이트 인용 부적격 |

정상 확인(반려 아님): 00a·00d 전 수치 재현(바이트 길이까지), 00b 차단 정직성, VKOSPI 값 타당성
(KOSPI 실현변동성 100% 환경 정합), SSOT 2키 무영향.

**Advisor 처리 필요 6건(비평가 식별)**: A-1 preview_coverage_min 0.80 ↔ 구조적 결측 충돌(ECOS
차단 시 상한 0.792<0.80 — 매 틱 억제. ECOS 키 발급 시 0.847로 해소) / A-2 K-18 Stooq 무효
(문구 개정·04a 스텁·fred.series 추가 허가·무폴백 4계열 GM1 기록) / A-3 VKOSPI 스코프 명시+
M-19(c) 구현 제약 명문화(패리티 보호) / A-4 credit 축 이중 차단 GM1 기록 / A-5 00c 계약 결함
3건 — 벤더링 시 fromJson 수정+PROVENANCE 등재 / A-6 미인도 2건(00g range 상한→MT1-06b 블록
유지, 00b FRED 절반→04b·04d 블록 유지).
**[사후 정정 2026-08-08 — MT1-04d aaa 판정]**: 위 A-1의 0.792/0.847은 산술 오류였음이 SSOT
전수 산출로 반증됨(어느 분모로도 도출 불가). 실측 정본: 현행 상한 27.5/31.0=**0.8871 ≥ 0.80**
(상시 억제 불성립 — 프리뷰 정상 동작), ECOS 지표 활성화 시 0.9516, 실제 억제는 런타임 결측
조합(MOVE+VIX3M → 0.7581)에서만. 이 오류 수치가 W3~W5 문서·KDoc·스모크 S-3 기대값에 전파돼
04d 라운드 1 FAIL(M-1)로 일괄 정정됨. 지표 활성화는 C1 소관 별도 지위(분모 82.5→88.5 —
골든 재산출 산술 확정).

처리: 00c(D-1·D-2+A-3, 원시 JSON 증거 의무) / 00g(D-3·D-4·D-5+A-6① — 다행 픽스처 뮤턴트
사망 확인 조건) / 00a(A-6② FRED HY OAS·T10Y2Y 실측) 재위임. A-1은 ECOS 키 발급(사용자 조치
대기)으로 우선 해소 경로 — 불가 시 MT1-07 착수 전 D-23 개정 상신. GM1 기록 항목은 PROGRESS
W0 절에 누적.

**라운드 2 (2026-08-07)**: 수정 3커밋(38a8ab7·d569cab·2a32f84) — qa **PASS**(뮤턴트 절차 독립
재현·D-1 수치 raw JSON 재계산) → aaa **PASS**(비평가 독립 재현: 뮤테이션 8종 재실행 M1~M7 전건
사망, range·FRED 수치 전건 일치, "3년 롤링"은 FRED 메타 축자 인용 확인 — 추측 판정 재발 없음.
신규 결함 0). 판정 대상 5과업(00a·00c·00d·00g 부분·SSOT 2키) 완료 처리. **웨이브 완료 선언
불가**: 00e(툴체인 매트릭스, 01a 블로커)·00f(SQLite 플랜) 미착수 적발 — 착수 지시. 00g의
raw JSON 컨테이너 키 정정("output"), A-1 재계산 0.847 검산 일치. 부기: 00b 저널에 FRED 절반
이관 상호참조 1줄 권고 — Advisor 반영.

### 배치 MT1-00e·00f·02a — 커밋 cdebe47·d6c39c4·a05259f / 4039bb6 / 3b5ddf0·daad43f

**라운드 1 (2026-08-07)**: qa **FAIL 3건**(PROGRESS 표기 실효 / 02a 커버리지 이탈 근거 저장소
부재 / 00e compileSdk 근거 문장 불일치) → 정정(28a64da·daad43f·a05259f) → qa 델타 **PASS** →
aaa **CONDITIONAL(경미 2)**. aaa 독립 재현: 02a contracts 뮤테이션 11종(9 사망·2 생존 적발 —
Severity·distinct_axes 완화 방향 증인 부재), "재생성이 가드를 무력화하지 못함" 확인(invalid
하드코딩 설계 정당), 00e 스모크 재실행·jvmToolchain 함정 축자 재현·버전 7종 Maven/Google
독립 대조(Room 3.x 부재 추가 확인), 00f 플랜·자가검증 재실행 일치.
경미 2건: ① 02a 다이제스트 제외 근거 과잉 서술(생존 뮤턴트 2종) ② 00e Kover 0.9.8 "최신
stable" 오기(실제 0.9.9). **해소 커밋**: ① 652d566(invalid 2건 추가 — 뮤턴트 2종 사망 확인
절차 동반, invalid 14파일·207 green) ② 2a4335b(Kover 0.9.9 정정+activityCompose 근거).
**해소 확인으로 PASS 승격** — 세 서브태스크 완료, PROGRESS 체크 승인. MT1-01a 착수 적격
(승계 3건: kotlin_krx 전이 의존성 버전 목록·카탈로그 앱 라이브러리 공백·wrapper 경로 미검증).

### MT1-01a mobile 스캐폴드 — 커밋 eb480b0·83c0e9e

**라운드 1 (2026-08-07)**: qa **PASS**(check·assembleDebug 재실행, 판단 6건 대조, SNAPSHOT 0,
계측 태스크 미포함, Python 207 무회귀) → aaa **CONDITIONAL(경미 1)**. aaa 독립 재현:
check --rerun-tasks 109/109, Kover 게이트 주입 시험 재현(+detekt 룰 실증 부수 확인), Konsist·
Robolectric XML 실측, snakeyaml-engine 3.1 POM 원문 대조(junit-jupiter-api compile 누출 = 상류
버그 실재), exclude 무부작용을 실제 configs 파싱 프로브로 증명(registry 0.3.1-rc·19지표 정상),
wrapper sha256 상류 대조 일치. 판단 6건 전건 정당. 경미 D-1: Robolectric @Config sdk=35 ↔
targetSdk 36 불일치·근거 부재 — **해소 83c0e9e**(sdk 36, 실행 재검증). **PASS 승격**.
부수 처분: FINAL §1.3 경로 `mobile/third_party/krxkt/`→`mobile/krx/` Advisor 정정(비평가
에스컬레이션 — B §3.2.1 Kover 글롭 연동), MT1-01f는 미충족·잔여 6건의 별도 서브태스크 확정,
01b·01g·02b/c 착수 적격(각 브리프 명시 요건: 01b ③건 / 01g 선결 ③건+00c 승계 의무 3건 /
02b :engine serialization 배선 누락 보완).

### W2 배치 MT1-03·04c·04ab — 커밋 4573e0c·eeee805·47fd335(+해소 c326428)

**라운드 1 (2026-08-07)**: qa **PASS**(207/207 재실행, tie-break 증인 실반전 재현, TRDVAL 픽스처
자릿수 대조, Stooq 스텁 실호출 불가 구조) → aaa **CONDITIONAL(경미 2)**. aaa 독립 재현: :lake
스키마 council 확정값 1:1(M-43·49·43b·34·22 전건), 증인 6종 뮤테이션 전건 사망(L1~L6), W1 생존
증인의 :lake 자동 보호 확인(B §3.2.1 lake 90% — W1 잔여 #2 해소), 04c 달력 재사용 등가성 소스
확인(getBusinessDays = 동일 엔드포인트·동일 파생), 04ab 커밋 비자기완결 주장은 전제 불성립
판정(dep는 04c 자신도 사용). 경미: D-1(RetryPolicy 리터럴 — W0 D-4 선례 구속, 즉시 해소 요구)·
D-2(Retrofit 선언·미사용 — 기술 선택은 재량 판정, 선언 정리). **해소 c326428**(assets 로더
fail-fast·필수 파라미터·Retrofit 제거·귀속 주석 정정) → **PASS 승격, W2 마감**.
Advisor 처분: A-2 기록 모순(sources.yaml 기변경 vs PROGRESS 미결 표기) → FINAL §4·PROGRESS
물질화(17efd8e). 후속 착수 요건: 04g(웜업 550 assets 로드·3년 롤링 구조적 부재 처리·공통
Collector 승격) / MT1-05(:engine 무의존·vkospi 폴백 유지·as_of 규약 대조 선행·브랜치 증인).

### W1 배치 MT1-01b·01g·02b·01f — 커밋 2103750·d47105b·ddd32f4·2299613

**라운드 1 (2026-08-07)**: qa **PASS**(check --rerun-tasks 115/115, 업스트림 2패스 정규화 diff —
로직 변경 3파일·미공개 0, TRDVAL 픽스처 저널 원시값 추적, 미러 12클래스 전수 대조, 증인 3종
재현) → aaa **CONDITIONAL(경미 1)**. aaa 독립 재현: 업스트림 토큰 diff(실변경 4파일 — qa의 3+
IndexFundamentalHistory 미사용 import, 전건 PROVENANCE 소급 확인), 무단 수정 가드
CHANGED/UNTRACKED 양방향, detekt 베이스라인의 신규 코드 미차폐를 위반 파일 주입으로 확인,
02b require() 뮤테이션 2종 결박 확인(역직렬화가 init 실행 사실 포함), 01b 자가치유 현상(B2)
실재 확인 — mustRunAfter 판단 정당, 01f 벤더 URL 파라미터화 = 적응 수정 범위 내 판정(대안은
게이트 하향뿐 — 그쪽이 규율 위반). 01a 잔여 6건 정산: 1·2(:contracts)·3·4 해소, 5 부분(→D-1),
6은 05e 대기. 경미 D-1: 측정 생존 증인이 :engine 한정 — :krx 과매치 글롭 침묵 통과 재현.
**01b·01g·02b 즉시 체크 승인, 01f는 D-1 해소 후.** W2 착수 적격 — 브리프 요건: MT1-03(:lake
모듈 신설 선행·M-43/49/43b/34 스키마 확정값·00f 승계·purge 분리) / MT1-04(04c 벤더 직접 수정
금지·K-03 간격 SSOT 배선·KOSPI 스코프·04a Stooq 무효 처리(A-2)·04b 계열별 파서 분리·04g 550).

### 실기기 S-0 결함 트랙 — 수정 4건 — 커밋 788f13f·9cbe115·a2c5e09 (2026-08-08)

구현 트랙 종결 선언(8839c15) 후, 실기기 S-0 선행(SM-F966N·Android 16)에서 사용자 보고
"API 키 입력 요소가 없다"로 개시. 동일 서브태스크 계열로 qa 8라운드·aaa 7라운드 수렴.

**결함 ①인셋·②INTERNET (788f13f)**: ① compileSdk 36 edge-to-edge 강제 + MainActivity 루트
인셋 미처리 → 탭 행이 상태바(89px) 뒤 — 터치·시인 불가(Advisor 3중 실측: bounds·input tap·
키 이벤트 대조). ② 매니페스트 INTERNET 미선언 → 전 소켓 SecurityException(사용자 검증 버튼
실증). 둘 다 JVM 원리적 미검출(인셋 재현 불가·테스트 네트워크 금지). 궤적: qa R1 FAIL(증인이
파일 전문 contains — 주석 자기언급으로 뮤테이션 생존) → 교정 → qa R2 PASS → aaa R1 **FAIL**
(D-1 imePadding은 무효 코드 — safeDrawing ⊇ ime 원본 반증 / D-2 배치 뮤턴트 C(리프 이동) 생존 /
D-3 선례 역인용) → imePadding 전면 제거+루트 체인 앵커 → qa R3 PASS → aaa R2 **COND**(C-1
"무해" 미실측 단정 — Advisor 문구 교정으로 종결, ④ 검증 범위 한정 물질화) → ② 발견·수정
(병합 매니페스트 blame으로 소스 단독 기여 확인, PackageManager 기반 증인) → qa R4 PASS →
aaa R3 **PASS**. 실기기 3경로 실측 통과(탭 y265>89+실탭 전환 / 최하단 필드 키보드 미가림 /
FRED HTTP 400 응답 = INTERNET 실증·SecurityException 부재).

**결함 ③자격증명 공백 미트림 (9cbe115)**: FRED 400의 오귀속 발단(후행 공백 재현 400 —
실제 진범은 ④). 1차 수정(save+검증 버튼) → qa R5 PASS → aaa R4 **FAIL**(D-4 생산 provider
3경로가 트림 우회 — 사용자 단말의 legacy 패딩 저장값에서 "버튼 초록·야간 배치 400" 조용한
실패 구조 / D-5 완료 조건 false green) → **load() 수렴점 재수정**(legacy 자동 치유) + save()
우회 raw prefs 증인 → qa R6 PASS(키 리터럴 어긋남 뮤테이션까지 자기방어 실증) → aaa R5
**PASS**("뮤턴트 1개가 provider 3종+isCollectConfigured+legacy를 동시 방어 — 수렴점 배치의
실증").

**결함 ④FRED realtime 시간대 (a2c5e09)**: buildUrl의 realtime_start/end가 UTC 날짜 → FRED
달력 기준 미래 날짜 400 — KST 오전 창 매일 실패(오전 캐치업·프리뷰), 17:00 확정 틱은 창 밖.
1차 수정 America/New_York → qa R7 PASS → aaa R6 **FAIL**(D-6: ET는 미실측 추정 — 03:30Z
재현은 ET/CT 무판별, 운영 주체는 세인트루이스 연준=CT, CT 참이면 13:00~14:00 KST 잔여창이
S-3(a) 슬롯과 정확히 중첩) → **Advisor 판별 실측 3점**: E1 03:30:57Z rt=08-08→400(UTC 반증) /
E2 04:06:10Z(ET 00:06) rt=08-08→**400(ET 반증)** / E3 05:06:09Z(CT 00:06) rt=08-08→**200** —
롤오버 (04:06Z,05:06Z] = UTC−5 = **America/Chicago 확정** → 존 교정+KDoc 실측표+경계 증인을
판별 시각 04:30Z(UTC/ET/CT 3자 분기)로 이동(UTC·ET 회귀 뮤턴트 양쪽 사망) → qa R8 PASS(시간대
산술 독립 재계산) → aaa R7 **PASS**(추론 폐쇄성 검산 — E3가 MT/PT 배제 역할까지 확인).
부수 적발: 기존 테스트 고정 clock 2026-08-06T00:00:00Z가 버그 창 안에서 UTC 계산을 정답
박제 → 정오 이동+경계 전용 증인 분리.

**구조 신호(aaa 확정 — GATE_GM1 전재)**: ⑴ 증인은 수정한 코드가 아니라 결함 도달 수렴점에
⑵ 고정 clock은 경계 밖 안전 시각에·경계는 전용 증인으로 ⑶ 외부 시스템 달력·시간대는 추정
금지·실측 필수(K-13 확장) ⑷ 실측은 반증 가능해야 — 무판별 관측 위의 오답이 기계 검증(qa)을
통과해 1라운드 출하됐음(기계 검증은 증인이 옳은 것을 겨냥했는지는 판정 못함) ⑸ 정량: 4건 중
2건(③④)은 JVM 검출 가능 — 종결 선언 조기의 정량 근거.

**잔여**: 재설치 후 사용자 "FRED 확인됨" 1회(실패 시 aaa R7 철회·재판정) / 겨울 CST 롤오버
1회 재실측(게이트 아님 — 후속 관찰) / UX: KIS appkey→appsecret 키보드 가림(imeAction=Next
부재) M2 이관(코드 주석 물질화).
