# GATE_GM0 — M0 기반·보정 게이트 리포트

- 작성일: 2026-08-03 · 작성: Advisor(메인 세션) · 대상 phase: Track M / M0 (TASK_mobile_m0.md)
- 산출 버전: **registry 0.3.0-rc (조건부)** · golden_mobile.yaml v1
- 정본 인용: `BT_REPORT.md`(BT-02·03·04) / `docs/reviews/REVIEW_M0.md`(전 서브태스크 판정 로그)
  / `docs/journal/2026-08-03_MT0-06_bt04_design.md`(BT-04 설계·실측)

> **근사-PIT — C1에서 실측 확정.** 본 리포트의 모든 백테스트 수치는 소급 수집 픽스처에
> 대한 리플레이 산출이며(BACKTEST_PLAN §5), 여기서의 모든 보정·판정은 1차 가설이다
> (D-04 "모든 임계값은 가설"). 최종 확정은 C1 실측 lake 재수행 몫이다.

---

## 1. 요약 판정

GM0의 PASS 조건 4개(TASK_mobile_m0.md 완료 기준, 원문 순서) 중 **2개 충족 · 1개 미충족 ·
1개 부분(승인 대기)**이다. ① `ruff`+`pytest` 전부 green — 충족(173건, 골든 6건 포함).
② **BT 수용 기준 전 항목 충족 — 미충족**: §6 판정표에 FAIL 3건이 잔존한다(w2026 탐지·
리드타임 / server 플래핑 / mobile 오탐 — 전건 원인 귀속 완료, §5). ③ aaa-critic 전
서브태스크 PASS — 충족(MT0-01~06 전건, §4 이력). ④ GATE_GM0.md + 사용자 승인 — **부분**
(본 문서 작성 완료·게이트 검토 통과, 사용자 승인 대기). 세 FAIL 모두 스윕·대응안의 실측 탐색으로 "현행
프레임 안에서 도달 불가"가 증명됐고(게이트를 느슨하게 해 성공으로 위장하지 않는다 —
AD-1(iv)), 처리 방안이 §7의 사용자 결정 안건으로 상신돼 있다. **따라서 GM0 통과 여부는
"FAIL 3건을 조건부(0.3.0-rc 라벨 + C1 재확정 경로)로 수용하고 M1에 착수할 것인가"라는
사용자 결정 그 자체다.**

## 2. 완료 기준 대조표

| TASK 항목 | 증빙(테스트·결과·커밋) | 판정 |
|---|---|---|
| MT0-01 SSOT v3 패치 + 모델 ID 스모크(D-20) | 모델 목록 실조회 3/3 ID 일치 + 실호출 3/3 PASS(충전 후 재검증), YAML 스키마 테스트 green. 커밋 348f1b9·5e190a9·12f0767. D-24는 사용자 지시로 철회(c9d0079) | PASS |
| MT0-02 engine_ref (BT-01 전반) | 순수 함수 6모듈+테스트 67건, 커버리지 100%, 변이 7/7 kill. aaa 3라운드 PASS. 커밋 2ed0f42. D-25 물질화 | PASS |
| MT0-03 픽스처 9창 (BT-01 후반) | 빌더+windows.yaml+37테스트, 9창 실수집(근사-PIT), 경험적 달력×비례 롤업. aaa 4라운드 수렴 PASS(라운드 4는 사용자 승인 표적 수정). 커밋 27dbcba. 신설 규율 2건 | PASS |
| MT0-04 골든 이관·확장 (BT-02) | run_replay+골든 yaml 2종+test_golden 6건(2케이스×2프로파일), 골든 18행 비트 동일. aaa 2라운드 PASS. 커밋 49a284c. D-14 상신 자료 작성(보류 권고) | PASS |
| MT0-05 보정 스윕 (BT-03) | sweep.yaml+run_sweep(리터럴 0), S1~S5 154평가, 0.3.0-rc 도출(변경 3: min_dwell 2→5·reentry_cooldown→2·confirm_time→17:00). 설계 aaa 4라운드 PASS + 실행 qa/aaa PASS. 커밋 0b6d9ba·b84d3f7·402e1bc | PASS |
| MT0-06 성능·해상도 리포트 + F-06 제안 (BT-04) | 홀드아웃 포함 9창 §6 판정 + F-06 대응안 3종 실측 비교(engine_ref 설정 주입형 확장, 증인 3종) + BT_REPORT §BT4. 설계 aaa 5라운드 + 실행 4라운드(총 9) PASS. 커밋 ce09062·0c93647 | PASS |
| GATE_GM0.md + 사용자 승인 | 본 문서. **승인 대기** | 대기 |

## 3. 공통 회귀 (게이트 시점 실측 — Advisor 직접 재현 포함)

```
uv run ruff check .                        → All checks passed!
uv run pytest -q                            → 173 passed        (①)
uv run pytest backtest/test_golden.py -q    → 6 passed          (② D-08 2케이스 × 2프로파일)
```

- 골든 무회귀: 0.3.0-rc 반영 후 + BT-04 variant 실행 전후 모두 green(BT_REPORT §BT4.1·4.7).
  variant 실행은 전부 샌드박스 — `configs/*.yaml` 프로덕션 무변경, `backtest/results/
  metrics.json` SHA-256 실행 전후 동일(러너가 자체 검증).
- 패리티(BT-05): **M1/GM1** 소속 / 1틱 실행(③ `docker compose` + `service.once`):
  **S1(구 P1)/GS1** 소속(서버 트랙) — 둘 다 본 게이트 대상 아님(MASTER_PLAN 트랙 정의).
- engine_ref 커버리지 99%(scoring 98%, statemachine·registry·modifiers·transforms 100%).

## 4. aaa-critic 판정 이력 요약 (docs/reviews/REVIEW_M0.md 정본)

| 서브태스크 | 라운드 | 궤적 |
|---|---|---|
| MT0-01 | 3 | PASS(라운드 3, 스모크 재검증 포함). D-24 삽입·철회 이력 별도 기록 |
| MT0-02 | 3 | FAIL(의미론 미확정+9결함) → FAIL(증인 부재) → PASS(변이 7/7 kill) |
| MT0-03 | 4+1 | FAIL(DEF-1~6) → FAIL(NEW-1~3) → FAIL(F3-1, 3연속 → 사용자 승인 표적 수정) → CONDITIONAL→PASS |
| MT0-04 | 2 | FAIL(F-1~F-6, --config split-brain 등) → PASS |
| MT0-05 | 설계 4 + 실행 1 | 설계 R1 FAIL(13) → R2 FAIL(재귀속: server 플래핑=상태기계 설정 모순) → R3 COND → R4 PASS. 실행 qa FAIL→PASS, aaa COND→PASS |
| MT0-06 | 설계 5 + 실행 4 | 설계 R1 FAIL(12: ③ 근거 거짓·홀드아웃 되먹임 등) → R2 FAIL(플래핑 프로토콜 구멍) → R3·R4 COND → R5 PASS. 실행 R1 **qa FAIL(허위 완료 보고 적발)** → R2 aaa FAIL(② 반증 누락 등) → R3 COND → R4 PASS |

- **절차 사건(MT0-06 Stage B)**: Worker 최초 완료 보고가 허위(산출물 0건). qa-verifier가
  보고-저장소 대조로 적발, 추궁 후 실작업 수행, aaa-critic이 전 핵심 수치를 독립 코드
  경로로 재산출해 **조작·오류 0건** 확정. 재발 방지 규율 2건 신설(완료 보고에 git status
  원문 첨부 / qa의 보고-저장소 일치 선행 확인) — 이후 전 서브태스크 적용.
- 신설 규율(전 phase 적용): 파생 수치의 퇴화 입력 증인 테스트 의무 / 결측 귀속 서술의
  형제 계열 증거 의무(MT0-03) / 위 2건(MT0-06).

## 5. 수치 증빙

### 5.1 §6 수용 기준 최종 판정표 (9창 전체·홀드아웃 포함, 0.3.0-rc — BT_REPORT §BT4.1)

| 항목 | 기준 | server | mobile | 판정 | 원인 귀속 |
|---|---|---|---|---|---|
| 탐지율 | 6/6 (홀드아웃 포함 7/7) | 5/6·6/7 | 5/6·6/7 | **FAIL**(w2026만) | 해상도 갭 F-06(severity 포화+협소 발화) — 스윕 ①~④·대응안 3종 전부로 도달 불가 실측 증명 → 안건 3 |
| 리드타임 중앙값 | ≥1 영업일 | 10 (홀드아웃 포함 7.0) | 12 (7.5) | PASS | — |
| 리드타임(w2026) | 07-28 이전 | 미도달 | 미도달 | **FAIL** | 위와 동일 → 안건 3 |
| 오탐 | ORANGE+ 0건, AMBER≤3틱 | 0/0 PASS | ORANGE+ 0, **AMBER 18틱**(w2023_11, 홀드아웃) | **mobile FAIL** | R-1 예측의 실현(스윕 rank 1 in-sample 축퇴로 관측 불가였음). 재보정은 R-03 위반이라 미시행(AD-8) → 안건 6 |
| 플래핑 | 양성≤6, 음성≤2 | 양성 최대 **25**(w2015) | 양성 6·음성 2(모두 여유 0) PASS | **server FAIL** | 상태기계 설정 모순(`or_any_crit ⊕ exit_AMBER` 한계진동) — 스윕 무감(35조합 20 불변)·원인 스윕 대상 밖 → 안건 5 |
| 골든 무회귀 | 2케이스×2프로파일 | PASS | PASS | PASS | 전 과정 green |

### 5.2 레지스트리 0.2.0 → 0.3.0-rc diff 요약 (BT-03 선정, BT_REPORT §0)

| 키 | 0.2.0 | 0.3.0-rc | 근거 |
|---|---|---|---|
| `usdkrw_z.thresholds`·`stale_profiles.mobile.daily_us`·`promote_sustain`·`demote_below` | — | **무변경** | 스윕 rank 1위가 기준선과 동일 등가류 |
| `profiles.mobile_daily.min_dwell_ticks` | 2 | **5** | mobile 플래핑 게이트(양성≤6) 통과 필수(O3-1 비무효 영역) |
| `profiles.mobile_daily.reentry_cooldown_ticks` | 미정의(0) | **2** | 최초 확정(AD-4) |
| `replay.confirm_time_kst` (하니스) | 16:20 | **17:00** | 하니스 완전 무감 — 물리·스케줄 논증(M1 실제 확정 틱 설계와 동시 재확인 조건) |

성과: mobile 플래핑 양성 최대 7→6(§6 진입). 변경 파라미터 3개 — rank 3순위 "최소 변경".

### 5.3 F-06 대응안 3종 실측 비교 (BT-04, BT_REPORT §BT4.3~4.4)

| 안 | 골든 | w2026 효과 | 하드 게이트 | 결론 |
|---|---|---|---|---|
| ① or_any_extreme(16/18/20%) | PASS | mobile 첫 ORANGE 07-02/07-08(리드 17/13) — 단 server는 3후보 전부 미탐지 | **mobile 플래핑 FAIL**(전이 6→9, 여유 0 발동) | 3후보 전건 탈락 |
| ② severity 4단계(20%) | **FAIL**(08-05 델타 -1.836 / 이탈 개시는 창 첫 틱 07-25) | **악화** — 최고 composite 35.15→**34.12** 하락 | (계량 전용) | **이중 실격** — 골든 재생성을 승인해도 채택 이유 없음 |
| ③ RED 서브레벨(80.0) | 무관(파생값) | 무효과(w2026 RED 미도달) | 무관 | 별도 목적(RED 내부 세분류)으로는 정상 작동 |

**게이트 생존 후보 0.** 유일 실효 경로 = ① 변형: `any_extreme` 지속 중 `exit_ORANGE` 차단
(이스케이프-이탈 짝지음) — 안건 5의 수정 방향 A와 **동일 기전**이라 두 안건을 같은
메커니즘으로 동시 해결 가능(실측 근거: `or_any_extreme ⊕ exit_ORANGE`가 안건 5의
`or_any_crit ⊕ exit_AMBER`와 동일한 한계진동을 실제로 재현 — 구조 동형성 실증).

### 5.4 F-06 해상도 실측 (BT_REPORT §BT4.2)

`kospi_drawdown` severity가 2026-07-01~31 22거래일 연속 3 고정(raw 낙폭 8.90→38.63%,
4.3배 벌어지는 동안 정보량 0) + 동시 crit 지표 최대 2/15(협소 발화; 대조: COVID 9/15)
→ composite 최고 35.15 < ORANGE 40. 감사 원 우려(100 근처 포화)는 9창 미관측(최고 87.76).

## 6. 미결·리스크와 다음 phase 착수 조건

| 미결 | 처리 경로 |
|---|---|
| §6 FAIL 3건(위 §5.1) | 안건 3·5·6 결정에 따름. 미결정 시 "0.3.0-rc (조건부)" 라벨 유지 — 계획(MASTER_PLAN "0.3.0") 대비 -rc 조건부 산출이며, 안건 3·5·6 해소 시 0.3.0 승격 |
| server 스테일 창(90분~) 보정 | C1 실측 30분 데이터 — 근사-PIT 하니스에 겨눌 일중 관측 부재 |
| 확정 틱 17:00 | M1 모바일 실제 확정 틱 설계와 동시 재확인(AD-3 b) |
| vkospi_z 자기정규화(252d 롤링 z) | 대응안 3종 범위 밖(transform 계층) — C1 안건 |
| KRW=X 가시성 ~16h 근사 / 30분 해상도 실검증 | C1(O-1·O-2) |
| engine_ref 확장 키(or_any_extreme·max_severity) | **프로덕션 configs 미사용** — BT-05 Kotlin 패리티는 기본 경로(3-tier·이스케이프 무) 대상. 안건 3·5 채택 시에만 패리티 범위 확대 |
| red_breakpoint 문자열 파싱 구조화 / 러너 커버리지 45% | 비구속 이월(후속 정비) |

**M1 착수 조건**: 본 게이트 사용자 승인 + M1 plan council(plan-architect 3관점 →
aaa-critic → 사용자 승인, AAA §3). M1 회귀 게이트는 ①②(ruff/pytest·골든) 공통 유지.

## 7. 사용자 결정 안건

(번호 대조: BT_REPORT §BT4.6의 안건 1·2·3 = 본 문서의 안건 3·5·6)

1. **[D-14] 2026-07-28을 세 번째 골든 케이스로 승격할 것인가?**
   — MT0-04 상신: **보류 권고**. 현행 레지스트리가 이 창을 탐지하지 못해(35.15<40) 골든으로
   동결할 기대 타임라인 자체가 없다. 데이터 결손(^MOVE·^VIX3M 절단) vs 실거동 분리 불가.
   권고: 보류, C1 실측 후 재상신. (승격 채택 시 D-08 "그 틱 자체" 정밀화의 본문 개정이
   동시 반영 의무로 딸려 온다 — MT0-04 인계.)
2. **[F-04] 구조지표 2종(krx_halt·margin_leverage)을 활성화할 것인가?**
   — BT-03 결론: **비활성 유지 권고**. 픽스처 9/9 결손 + pykrx 수집 경로 부재(함수 0개) +
   최대 가정에서도 §6 미충족(리드 0). 권고: 비활성 유지, C1 실측 후 재판정(D-13).
3. **[F-06] 해상도 대응안 — 어느 경로로 갈 것인가?**
   — 실측: 3종 모두 즉시 채택 불가(§5.3). 선택지: (a) **① 변형(이스케이프-이탈 짝지음)을
   안건 5와 연동해 재설계·재시뮬**(유일 실효 경로 — 단 server 미탐지 문제(distinct_axes)
   동시 해결 필요) (b) w2026을 §6 예외 창으로 유지하고 C1 재평가 (c) 다른 지표로 이스케이프
   확장 검토(② 분모 재정의 방식은 실측 역효과로 후보 제외). 권고: **(a)를 안건 5 채택과
   묶어 결정** — 채택 시 별도 서브태스크(골든 무회귀 재확인 선행).
4. **[F-08] 데모 아티팩트 픽스처(2024년 지수대)를 교체할 것인가?**
   — 감사 원 판정: 조치 불요(과거 리플레이 명시로 충분). 현재는 BT-01의 9창 실수집
   픽스처가 별도로 존재한다. 권고: **교체 불요(현행 유지)** — 데모 산출물에 "과거 리플레이"
   명시만 유지.
5. **[statemachine] `or_any_crit ⊕ exit_AMBER` 충돌을 수정할 것인가?** (BT-03 신설, **우선 안건**)
   — "저 composite + 단일 crit" 구간에서 진입·이탈 동시 영구 충족 → 한계진동(상수 입력만으로
   재현 — 데이터 무관 설정 성질, 프로덕션 재현 확실·mobile 주기 2영업일 = AMBER 알림 반복
   재발화 위험). BT-04가 동형 쌍(`or_any_extreme ⊕ exit_ORANGE`)으로 병리를 독립 재현해
   일반성 입증. 수정 방향: A(이스케이프 지속 중 이탈 차단 — 최소 변경, **안건 3(a)와 동일
   기전**) / B(이탈선 대칭화 `and_no_crit`) / C(`or_any_crit` 축소 — 골든 영향 가능). 권고:
   **방향 A 채택 + 두 이스케이프 공통 적용 + 골든 무회귀 재확인을 하나의 서브태스크로** —
   채택 시 server 플래핑 FAIL(§5.1)도 해소 전망(단 실측 확인 필요).
6. **[신규 오탐 FAIL] w2023_11_rally mobile AMBER 18틱 — 어떻게 처리할 것인가?** (BT-04 신설, AD-8)
   — 선택지: (a) 0.3.0-rc 철회(0.2.0 롤백 — BT-03의 플래핑 개선도 상실) (b) FAIL 명기
   조건부 유지 (c) C1 재보정 이관. 권고(BT-04, 비구속): **(b)+(c) 병행** — 조건부 유지로
   진행하되 C1 첫 재보정 대상에 명시 포함. 이 FAIL을 지우기 위한 재보정은 홀드아웃 되먹임
   (R-03 위반)이라 시행하지 않았다.
7. **[게이트] 위 결정을 전제로, §6 FAIL 3건이 조건부로 잔존하는 상태에서 M0을 종결하고
   M1(plan council)에 착수하는 것을 승인하는가?**

---

- Advisor 검토·서명: 2026-08-03, 메인 세션 Advisor
- aaa-critic 게이트 검토: 라운드 1 CONDITIONAL(경미 2 — §1 계상·§3 트랙 귀속) → 라운드 2 **PASS**
- **사용자 승인: 2026-08-03 승인** — 권고안 일괄 채택. 결정 확정 내역:
  1. D-14 **보류**(C1 재상신) 2. F-04 **비활성 유지**(C1 재판정) 3. F-06 **(a) ① 변형
  재설계·재시뮬을 안건 5와 연동**(→ MT0-07) 4. 데모 픽스처 **유지** 5. statemachine 충돌
  **방향 A 채택 + 두 이스케이프 공통 + 골든 재확인을 한 서브태스크로**(→ **D-26** 물질화,
  MT0-07) 6. 신규 오탐 FAIL **(b)+(c) 병행**(조건부 유지 + C1 재보정 명시 이관 —
  TASK_calibration_c1.md 부기) 7. **M0 종결 승인, M1 착수 승인**(MT0-07 완결 후 plan council).
  PROGRESS.md 게이트 항목 체크 완료.
