# TASK_mobile_m2 — 판단·다이제스트·UI (M2)

- 선행: GM1 승인 · 게이트: **GM2** · plan council 필수(UX 관점 플래너가 화면 흐름 명세를 선행 산출)
- 디자인 규격: `.claude/skills/design-system` — 모든 UI 서브태스크 브리프에 이 스킬 참조를 명시한다.

## 서브태스크

### MT2-01 evidence 조립 (모바일)
evidence-pack/1을 Room 데이터로 조립(fired_indicators·market·macro_calendar_7d; news/analogues는 v1 빈 리스트 — 계약상 유효).
**SSOT 신규 허가**: `configs/macro_calendar.yaml`을 본 서브태스크에서 생성한다(FOMC·금통위·주요 지표 일정 — 서버 S3의 ST-P3-01이 동일 파일을 승계 소비). syncConfigs 동기화 대상에 추가.
**G-1 완화(D-22)**: `configs/analogue_seed.yaml`을 assets 동기화 대상에 포함하고, evidence의 `analogues`를 빈 리스트로 두는 대신 **시드 메타데이터 기반 후보 3건**(앵커일·성격·경로 요약)을 임베딩 검색 없이 규칙 매칭(이벤트 유형 + 국면 수준)으로 채운다. 유사도 점수는 계산하지 않으므로 `similarity`는 0.0으로 두고 프롬프트에 "시드 인용(비검색)"임을 명시한다.
완료: 계약 검증 테스트(스냅샷 대조) green + 시드 인용 경로 테스트 green.

### MT2-02 LLM 계층
Claude API 직결: 구조화 출력(scenario-snapshot/1 json schema), 프롬프트는 assets의 prompts/*.md만 로드(하드코딩 금지), 티어링은 statemachine.yaml 값 사용(D-20). 재시도·타임아웃·비용 카운터(월 사용량 표시). 일일 다이제스트는 확정 틱 후 자동 1회, 시나리오 리포트는 ORANGE/RED에서 **명시 버튼**으로만. **리포트 버튼은 확정 스냅샷 기준일 때만 활성**(프리뷰 상태에서는 비활성 — D-23 §23.3-5). `llm_tiering.scenario_report.web_search` 값을 서버와 동일하게 소비하는지 확인한다(서버측 도구 실행이므로 모바일에서도 동작).
완료: 모의 서버 기반 계약 테스트 + 품질 게이트(진폭 밴드·무효화 판정가능성·확률 합 — 기존 ST-P3-03 규칙 이식) green.

### MT2-03 디자인 시스템 구축
Tokens.kt(색·타이포·간격·모션 — 국면 팔레트 이중 부호화 포함), 공용 컴포넌트(PhaseBadge, IndicatorChip, StaleBadge, PreviewBanner, SkeletonCard, TrendChart). 다크/라이트, 대비 AA 검증 스크립트.
완료: 컴포넌트 프리뷰 카탈로그 화면 + 토큰 리터럴 스캔 0건.

### MT2-04 홈 대시보드
3초 규칙: 국면·composite 게이지·상위 발화 지표 3개·데이터 as_of가 첫 화면에서 즉시 파악. 프리뷰 모드에서는 **coverage %와 이월(스테일) 지표 수**를 게이지 옆에 상시 표기(D-23). 축별 지표 그리드(심각도 정렬), 국면 히스토리 스트립(최근 60틱), 당겨서 새로고침=프리뷰, 스켈레톤 로딩.
완료: macrobenchmark(콜드 스타트<2s, 정크<1%), 상용 대비 검사(§2.5) PASS.

### MT2-05 리포트·다이제스트 화면
일일 다이제스트 카드(5문장 규칙 렌더), 시나리오 리포트 뷰(시나리오 카드·확률 서열·무효화 조건·리드 지표·품질 게이트 배지 — 데모 아티팩트 레이아웃 이식), 히스토리 목록·검색.
완료: 스냅샷 UI 테스트, 장문·결측·게이트 FAIL 상태 렌더 확인.

### MT2-06 위젯·설정·온보딩
Glance 홈 위젯(국면 색·composite·마지막 틱), 설정(키 입력·틱 시각·백업·KIS 토글·데이터 내보내기), 온보딩(키 발급 안내, OEM 절전 예외 안내 K-15, **v1 한계 고지**: 일 1회 확정·장중 감시 없음 + **D-22 §22.2의 G-1~G-3**(유사사례 축소·뉴스 축 비활성·HMM 부재) 명시 — 리포트 진폭 추정을 서버판과 동일 신뢰도로 읽지 않도록).
완료: 위젯 갱신<1s, 온보딩 E2E 체크리스트 통과.

### MT2-07 접근성·완성 요소
TalkBack 라벨, 글꼴 스케일 1.3× 무파손, 적응형 아이콘·스플래시·빈/오류 상태 그래픽 제작.
완료: 접근성 스캐너 0 크리티컬, aaa-critic §2.4 전 항목 PASS.

## 완료 기준 (GM2)
전 테스트 green + 골든·패리티 무회귀 / §2.4·§2.5 전 항목 PASS / 실기기 시연 영상 첨부 / GATE_GM2.md + 사용자 승인.
비용 확인: 일일 다이제스트 30일 예상 비용 산출표(캐싱 반영) 첨부 — 월 상한 내 증명.
